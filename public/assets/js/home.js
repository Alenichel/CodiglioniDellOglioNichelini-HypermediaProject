"use strict";

function build_person_row(name, img, i){

}

$(document).ready(function() {

    /*fetch('/api/v1/people').then(response => {
        response.json().then( json => {
            for (let s of json){
                let fs = s.firstName
                let img = s.picture
                $('#people-row').append(build_person_row(fs, img));
            }
        })
    })*/
})